Used float t = GetTime() and DrawCircle(400 + 200 * sin(t), 225, 40, RED); to make the circle move from left to right. 

Next step is drawing a basic solar system, about to layer DrawCircle with different coordinates

Next steps: Got the sun in the middle of the screen, now I just need to make the other 2 planets orbit the sun in the center, then I can add the rest of the solar system